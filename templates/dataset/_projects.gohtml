{{ if not .IsHTMXRequest }}<div class="card card-collapsible mb-6 dataset-projects">{{ end }}
    <div class="card-header">
        <div class="collapse-trigger collapsed" data-toggle="collapse" data-target="#collapse-projects" aria-expanded="false" aria-controls="collapse-projects"></div>
        <div class="bc-toolbar">
            <div class="bc-toolbar-left">
                <i class="if if-chevron-right mr-4 text-muted"></i>Projects
            </div>
            <div class="bc-toolbar-right">
                {{if .User.CanEditDataset .D.Dataset}}
                <div class="c-button-toolbar" data-panel-state="read">
                    <button class="btn btn-outline-primary"
                        hx-get="{{pathFor "dataset_projects" "id" .D.Dataset.ID}}"
                        hx-target="#datasets-projects-modal"
                        hx-trigger="click"
                    >
                        <i class="if if-add"></i>
                        <div class="btn-text">Add project</div>
                    </button>
                </div>
                {{end}}
            </div>
        </div>
    </div>

    <div class="collapse {{ if .IsHTMXRequest }}show{{ end }}" id="collapse-projects">
        <div class="card-body p-0">
            <table class="table">
                <tbody>
                    {{ $datasetID := .D.Dataset.ID }}
                    {{ range $k, $v := .D.Dataset.Project }}
                    <tr>
                        <td class="pl-6">
                            <div class="d-flex w-100">
                                <div class="w-100">
                                    <div class="d-flex align-items-start">
                                        <img class="list-group-item-thumbnail" src="{{assetPath "/images/dataset-1.jpg"}}" height="96">
                                        <div class="list-group-item-content">
                                            <div class="list-group-item-text">
                                                <h4 class="c-h4">{{ $v.Name }}</h4>
                                                <div class="c-body-small text-muted">Description</div>
                                                <div class="text-muted c-body-small">Project ID: {{ $v.ID }}</div>
                                            </div>
                                        </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </td>
                        <td class="pr-6">
                            {{if $.User.CanEditDataset $.D.Dataset}}
                            <div class="c-button-toolbar">
                                <div class="dropdown">
                                    <button class="btn btn-link btn-icon-only btn-link-muted" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                        <i class="if if-more"></i>
                                    </button>
                                    <div class="dropdown-menu">
                                        <button class="dropdown-item" type="button"
                                            hx-get="{{pathFor "dataset_projects_confirm_remove" "id" $datasetID "project_id" $v.ID}}"
                                            hx-target="#datasets-projects-modal"
                                            hx-trigger="click"
                                        >
                                            <i class="if if-delete"></i>
                                            <span>Remove from dataset</span>
                                        </button>
                                    </div>
                                </div>
                            </div>
                            {{end}}
                        </td>
                    </tr>
                    {{ end }}
                </tbody>
            </table>
        </div>
    </div>

    <div id="datasets-projects-modal" class="modals"></div>
{{ if not .IsHTMXRequest }}</div>{{ end }}
