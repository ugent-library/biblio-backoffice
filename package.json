{
    "private": true,
    "scripts": {
        "dev": "npx nodemon"
    },
    "devDependencies": {
        "nodemon": "^2.0.7"
    },
    "nodemonConfig": {
        "exec": "go run main.go server start --host localhost --port 3001 || exit 1",
        "signal": "SIGTERM",
        "ext": "go,gohtml,json",
        "delay": "1500"
    }
}
