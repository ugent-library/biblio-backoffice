package views

templ ShowModal(component templ.Component) {
	<div id="modal-backdrop" class="d-block modal-backdrop fade show"></div>
	<div id="modal" class="d-block modal show" tabindex="-1" aria-modal="true" role="dialog">
		@component
	</div>
}
