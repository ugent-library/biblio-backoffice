<div class="modal show" tabindex="-1" style="display: block;" aria-modal="true" role="dialog">
    <div class="modal-dialog modal-dialog-centered modal-lg modal-dialog-scrollable" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">Assign UGent membership to {{.D.Role}}</h2>
            </div>
            <div class="modal-body">
                {{if .D.Suggestions}}
                {{range $.D.CreditRole}}
                <input type="hidden" name="credit_role" value="{{.}}">
                {{end}}
                <ul class="list-group mt-6">
                    {{range .D.Suggestions}}
                    <li class="list-group-item" id="person-{{.ID}}">
                        <div class="d-flex w-100">
                            <div class="w-100">
                                <div class="d-flex align-items-start">
                                    <div class="list-group-item-content">
                                        <div class="list-group-item-text">
                                            <h4 class="c-h4">{{.FullName}}</h4>
                                            {{range .Department}}
                                            <div class="text-muted c-body-small">{{$.T "organization" .ID}}</div>
                                            {{end}}
                                            {{with .ORCID}}
                                            <div class="text-muted c-body-small">ORCID: {{.}}</div>
                                            {{end}}
                                            {{range .UGentID}}
                                            <div class="text-muted c-body-small">UGent ID: {{.}}</div>
                                            {{end}}
                                            <input type="hidden" name="id" value="{{.ID}}">
                                        </div>
                                        <div class="list-group-item-meta mt-2">
                                            <div class="list-group-item-meta-left">
                                            </div>
                                            <div class="list-group-item-meta-right">
                                                <button class="btn btn-primary membership-member"
                                                    hx-put="{{pathFor "publication_contributors_promote" "id" $.D.Publication.ID "role" $.D.Role "position" ($.D.Position | toString)}}"
                                                    hx-target="#{{$.D.Role}}-modal"
                                                    hx-trigger="click"
                                                    hx-swap="innerHTML"
                                                    hx-include='#person-{{.ID}} input[name="id"], .modal-body input[name="credit_role"]'
                                                >Confirm {{$.D.Role}}</button>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </li>
                    {{end}}
                </ul>
                {{else}}
                This {{.D.Role}} is not a UGent employee or student.
                {{end}}
            </div>
            <div class="modal-footer">
                <button class="btn btn-link membership-external" data-target="#{{.D.Role}}-modal" data-dismiss="modal"
                    {{if (gt (len (.D.Publication.Contributors .D.Role)) .D.Position)}}
                    hx-get="{{pathFor "publication_contributors_edit" "id" .D.Publication.ID "role" .D.Role "position" (.D.Position | toString)}}"
                    {{else}}
                    hx-get="{{pathFor "publication_contributors_add" "id" .D.Publication.ID "role" .D.Role "position" (.D.Position | toString)}}"
                    hx-include='.modal-body input[name="first_name"], .modal-body input[name="last_name"], .modal-body input[name="credit_role"]'
                    {{end}}
                    hx-target="#{{.D.Role}}-modal"
                    hx-trigger="click"
                    hx-swap="innerHTML"
                >Cancel</button>
            </div>
        </div>
    </div>
</div>
